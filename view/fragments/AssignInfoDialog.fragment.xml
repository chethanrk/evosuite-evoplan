<core:FragmentDefinition
        xmlns="sap.m"
        xmlns:l="sap.ui.layout"
        xmlns:table="sap.ui.table"
        xmlns:core="sap.ui.core"
        xmlns:f="sap.ui.layout.form">

    <Dialog title="{i18n>xtit.assignInfoModalTitle}">
        <content>
            <f:SimpleForm id="assignmentInfoForm"
                          editable="true"
                          layout="ResponsiveGridLayout"
                          labelSpanXL="3"
                          labelSpanL="3"
                          labelSpanM="3"
                          labelSpanS="12"
                          emptySpanXL="4"
                          emptySpanL="4"
                          emptySpanM="4"
                          emptySpanS="0"
                          columnsXL="1"
                          columnsL="1"
                          columnsM="1"
                          singleContainerFullSize="false" >
                <f:content>
                    <Label text="{i18n>xfld.demandName}" />
                    <Text text="{Description}"/>
                    <Label text="{i18n>xfld.startDate}" />
                    <DatePicker
                        valueFormat="yyyy-MM-dd"
                        value="{StartDate}"/>
                    <Label text="{i18n>xfld.endDate}" />
                    <DatePicker
                            valueFormat="yyyy-MM-dd"
                            value="{EndDate}"/>
                    <Label text="{i18n>xfld.status}" />
                    <ComboBox
                            items="{
                            path: '/StatusTextsSet'
                            }">
                        <core:ListItem key="{UserStatusShortText}"
                                       text="{UserStatusLongText}"/>
                    </ComboBox>
                </f:content>
            </f:SimpleForm>

        </content>

        <beginButton>
            <Button text="{i18n>xbut.buttonClose}" press="onModalCancel" />
        </beginButton>

        <endButton>
            <Button text="{i18n>xbut.buttonSave}" press="onModalSaveAssignInfo" />
        </endButton>
    </Dialog>

</core:FragmentDefinition>