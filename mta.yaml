_schema-version: "3.2"
ID: comevoraitevoplan
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: comevoraitevoplan-destination-content
  type: com.sap.application.content
  requires:
  - name: comevoraitevoplan-destination-service
    parameters:
      content-target: true
  - name: comevoraitevoplan-repo-host
    parameters:
      service-key:
        name: comevoraitevoplan-repo-host-key
  - name: comevoraitevoplan-uaa
    parameters:
      service-key:
        name: comevoraitevoplan-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: comevoraitevoplan_html_repo_host
          ServiceInstanceName: comevoraitevoplan-html5-srv
          ServiceKeyName: comevoraitevoplan-repo-host-key
          sap.cloud.service: comevoraitevoplan
        - Authentication: OAuth2UserTokenExchange
          Name: comevoraitevoplan_uaa
          ServiceInstanceName: comevoraitevoplan-xsuaa-srv
          ServiceKeyName: comevoraitevoplan-uaa-key
          sap.cloud.service: comevoraitevoplan
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: comevoraitevoplan-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: comevoraitevoplan-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - comevoraitevoplan.zip
      name: comevoraitevoplan
      target-path: resources/
- name: comevoraitevoplan
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: comevoraitevoplan-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: comevoraitevoplan-destination-service
    service-plan: lite
- name: comevoraitevoplan-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: comevoraitevoplan-xsuaa-srv
    service-plan: application
- name: comevoraitevoplan-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: comevoraitevoplan-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
