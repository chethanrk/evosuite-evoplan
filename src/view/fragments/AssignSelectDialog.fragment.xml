<core:FragmentDefinition
        xmlns="sap.m"
        xmlns:l="sap.ui.layout"
        xmlns:table="sap.ui.table"
        xmlns:core="sap.ui.core">

    <Dialog title="{i18n>xtit.assignModalTitle}">
        <content>
            <Toolbar>
                <SearchField liveChange="onSearchAssignModal" width="100%" />
            </Toolbar>

            <table:TreeTable id="assignModalTable"
                    selectionMode="Single"
                    enableColumnReordering="false"
                    expandFirstLevel="false"
                    columnHeaderVisible="false"
                    rowSelectionChange="onSelectionChangeAssignModal"
                    rows="{path : '/TechnicianSet'}">

                <table:columns>
                    <table:Column label="Description">
                        <table:template>
                            <Text text="{Description}" wrapping="false" />
                        </table:template>
                    </table:Column>
                </table:columns>
            </table:TreeTable>
        </content>

        <endButton>
            <Button text="Cancel" press="onCancelAssginModal" />
        </endButton>
    </Dialog>

</core:FragmentDefinition>