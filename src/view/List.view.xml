<mvc:View
        xmlns="sap.m"
        xmlns:mvc="sap.ui.core.mvc"
        xmlns:core="sap.ui.core"
        xmlns:f="sap.f"
        xmlns:smartTable="sap.ui.comp.smarttable"
        xmlns:semantic="sap.m.semantic"
        xmlns:table="sap.ui.table"
        controllerName="com.evorait.evoplan.controller.List">

    <Page id="listPage"
          title="Demands"
          showFooter="true"
          showHeader="true">

        <f:DynamicPage headerExpanded="true"
                       preserveHeaderStateOnScroll="true"
                       fitContent="true"
                       showFooter="false"
                       class="tableContentPage">

            <f:header>
                <f:DynamicPageHeader pinnable="true">
                    <f:content>
                        <core:Fragment fragmentName="com.evorait.evoplan.view.fragments.SmartFilter" type="XML"/>
                    </f:content>
                </f:DynamicPageHeader>
            </f:header>

            <f:content>
                <smartTable:SmartTable
                        id="draggableList"
                        height="100%"
                        width="100%"
                        direction="Column"
                        fitContainer="true"
                        enableAutoBinding="true"
                        entitySet="{viewModel>/tableEntity}"
                        smartFilterId="listReportFilter"
                        showRowCount="true"
                        header="{viewModel>/tableTitle}"
                        useExportToExcel="true"
                        useVariantManagement="true"
                        useTablePersonalisation="true">
                    <!-- layout data used to make the table growing but the filter bar fixed -->
                    <smartTable:layoutData>
                        <FlexItemData growFactor="1" />
                    </smartTable:layoutData>
                </smartTable:SmartTable>
            </f:content>

        </f:DynamicPage>

        <footer>
            <OverflowToolbar id="otbFooter">
                <Button icon="sap-icon://message-popup" text="{/messagesLength}" type="Emphasized"
                        press="handleMessagePopoverPress"/>
                <ToolbarSpacer/>
                <Button text="Assign" icon="sap-icon://activity-individual" press="onAssignButtonPress">
                    <layoutData>
                        <ToolbarLayoutData shrinkable="true" />
                    </layoutData>
                </Button>
            </OverflowToolbar>
        </footer>

    </Page>
</mvc:View>